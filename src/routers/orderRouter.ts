import express, { Router } from "express";
// import { fetchUserOrder, fetchUserOrderById } from "../controllers/orderController.js";

const router: Router = express.Router();

router
   // .get("/", fetchUserOrder)
   // .get("/:orderId", fetchUserOrderById)

export default router;